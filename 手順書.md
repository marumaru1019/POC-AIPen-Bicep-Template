## リソースグループの作成
リソースグループ名: 任意
リソースグループの場所: japaneast

## app service (漫画作成用) の作成
リソースグループ: 上記で作成したリソースグループ
アプリ名: 任意
ランタイムスタック: Python 3.11
ランスタックのバージョン: 3.11
リージョン: japaneast
App Service プラン: 新規作成
SKU: P0V3
appilcation insights: 有効

## 設定
### スタートアップコマンド
gunicorn --bind=0.0.0.0 --timeout 600 generate_html:app

### 環境変数
SCM_DO_BUILD_DURING_DEPLOYMENT: true

## app service (OpenAI アクセス用) の作成
リソースグループ: 上記で作成したリソースグループ
アプリ名: 任意
ランタイムスタック: Python 3.11
ランスタックのバージョン: 3.11
リージョン: japaneast
App Service プラン: 漫画作成用と同じものを使用
SKU: P0V3
appilcation insights: 有効

## azure openai service の作成
すでにあればそれを使用

## 設定
### スタートアップコマンド
gunicorn -k uvicorn.workers.UvicornWorker --bind=0.0.0.0 --timeout 600 app.main:app

### 環境変数
SCM_DO_BUILD_DURING_DEPLOYMENT: true
GENERATE_HTML_URL: <app-service-url>/generate_html
AZURE_OPENAI_API_KEY: api management のサブスクリプションキー
AZURE_OPENAI_ENDPOINT: apim のエンドポイント
DALL_E_API_KEY: api management のサブスクリプションキー
DALL_E_URL: api management のエンドポイント

### CORS の設定
CORS:
- UI用の app service の URL


## app service (UI用) の作成
リソースグループ: 上記で作成したリソースグループ
アプリ名: 任意
ランタイムスタック: Node.js
ランスタックのバージョン: 20 LTS
リージョン: japaneast
App Service プラン: 漫画作成用と同じものを使用
SKU: P0V3
appilcation insights: 有効

## 設定
### スタートアップコマンド
node server.js

### 環境変数
PORT: 3000
SCM_DO_BUILD_DURING_DEPLOYMENT: false


## アプリケーションのデプロイ (漫画作成)
### zipファイルの作成
```bash
cd src
zip -r ../app.zip .
cd ..

### App Serviceへのデプロイ
az webapp deployment source config-zip \
  --resource-group rg-aipen-sample \
  --name app-aipen-generative-html \
  --src app.zip
```

## アプリケーションのデプロイ (OpenAI アクセス用)
### zipファイルの作成
```bash
cd AIPen-Backend
zip -r ./app.zip .
```

### App Serviceへのデプロイ
```bash
az webapp deployment source config-zip \
  --resource-group rg-aipen-sample \
  --name app-aipen-openai \
  --src app.zip
```

